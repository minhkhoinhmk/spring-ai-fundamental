package com.nhmk.agentic_example.infrastructure.conversation;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Tests for the legacy ConversationService have been disabled because the
 * application uses Spring AI chat memory now. Kept for historical purposes.
 */
@Disabled("ConversationService replaced by Spring AI ChatMemory; tests disabled")
class ConversationServiceTest {

    @Test
    void placeholder() {
        // intentionally disabled
    }
}
